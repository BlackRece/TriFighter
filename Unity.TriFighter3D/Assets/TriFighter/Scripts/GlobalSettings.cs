using UnityEngine;

namespace TriFighter {
    [CreateAssetMenu(
        menuName = "TriFighter Objects/Create GlobalSettings",
        fileName = "GlobalSettings",
        order = 0
    )]
    public sealed class GlobalSettings : ScriptableObject {
        
    }
}